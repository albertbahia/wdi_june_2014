<h2>POSTS</h2>

<% @entries.each do |entry| %>
<h3><%= entry.title %></h3>
<p><%= entry.content %></p>
<%= link_to("EDIT POST", edit_entry_path(entry)) %>
<% end %>
<p><%= link_to("ADD POST", new_entry_path) %></p>
