//* Write a function to accept an array and print the elements in order
randomArray = [1, "tea", "someone"]

var printArray = function (array) {
  for (var i = 0; i < array.length; i++) {
    console.log(array[i]);
  }
}

printArray(randomArray);
