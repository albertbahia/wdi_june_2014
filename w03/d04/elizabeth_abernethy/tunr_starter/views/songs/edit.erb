<h2>New Song</h2>

<form action="/songs/<%= @songs.id %>" method="post">
  <label for="song[title]">Title:</label>
  <input name="song[title]" value="<%= @songs.title %>">

<form action="/songs/<%= @songs.id %>" method="post">
  <label for="song[album]">Album:</label>
  <input name="song[album]" value="<%= @songs.album %>">

<form action="/songs/<%= @songs.id %>" method="post">
  <label for="song[preview_url]">Preview URL:</label>
  <input name="song[preview_url]" value="<%= @songs.preview_url %>">

<form action="/songs/<%= @songs.id %>" method="post">
  <label for="song[price]">Price:</label>
  <input name="song[price]" value="<%= @songs.price %>">

  <input type="submit" value="Create">
</form>
