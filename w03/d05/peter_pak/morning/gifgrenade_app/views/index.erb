  <h2>currently trending gifs</h2>
    <% @gif_time['data'].each do |array| %>
    <ul>
    <li><a href="<%= array['url'] %>" target="_blank">
    <img src="<%= array['images']['fixed_height']['url'] %>"></a>
      <code><%= array['images']['fixed_height']['url'] %></code>
    <br></li>
  </ul>

    <% end %>
