
<h2>Songs</h2>

<a href="/songs/new">Add Song</a>

<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="/songs/<%= song.id %>">
        <%= song.title %> - <%= artist.name.where(song.artist_id = artist.id) %>
      </a>
    </li>
  <% end %>
</ul>
