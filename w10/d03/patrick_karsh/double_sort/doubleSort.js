function dubSort (array) {
  array.sort();
  var wordArray = []; 
  for (var i = array.length - 1; i >= 0; i--) {
    if (array[i] = isNaN()) {
      wordArray.push(array[i]); 
    }
  };
  array.upshift(wordArray);

  console.log(Array);  
}