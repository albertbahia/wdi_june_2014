var productsList = $('#products-list');
var products = $('<%= j(render @products) %>');
productsList.empty();
var filteredProducts = products.appendTo(productsList);
filteredProducts.hide().slideDown(400);
