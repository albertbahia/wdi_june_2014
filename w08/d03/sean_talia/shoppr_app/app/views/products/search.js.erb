<% if @products.length == 0 %>
  $('#products-list').text("No Results Found");
<% else %>
  $('#products-list').text('');
  var productHtml = '<%= j(render @products) %>';
  var productDiv = $(productHtml);
  $('#products-list').append(productDiv).slideUp().slideDown();
<% end %>
