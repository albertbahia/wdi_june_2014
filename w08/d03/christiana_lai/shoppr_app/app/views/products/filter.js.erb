var productsDiv = $('#products-list');
var productsHtml = '<%= j(render @products) %>';
var products = $(productsHtml);
productsDiv.empty();
products.appendTo(productsDiv);
products.hide().slideDown(1000);
