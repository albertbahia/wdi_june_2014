<h1>Movies List</h1>

<p><%= link_to('Add Movie', new_movie_path(@movie)) %></p>

<div>
<% @movies.each do |movie| %>
    <p><%= movie.title %></p>
    <p><%= movie.year %></p>
    <p><%= movie.plot %></p>
    <%= link_to image_tag(movie.poster_url), movie_path(movie) %>
<% end %>
</div>
