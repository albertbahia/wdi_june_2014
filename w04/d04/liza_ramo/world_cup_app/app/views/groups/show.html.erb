<h1><%= @group.name %></h1>

<ul>
  <% @group.teams.each do |team| %>
    <li>
      <%= link_to("#{team.country}", team_path(team)) %>
    </li>
  <% end %>
</ul>

<p><%= link_to('Edit', edit_group_path(@group)) %></p>
<p><%= link_to('Delete', group_path(@group), method: :delete) %></p>
