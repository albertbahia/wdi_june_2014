<%= link_to 'All Players', players_path %>
<h1><%= @player.name %></h1>

<p><%= link_to "Edit a Player", edit_player_path(@player) %></p>
<p><%= link_to "Add New Player", players_path(@player) %></p>


<% @player.each do |player| %>
  <div>
    <p><%=link_to player.name, player_path(player) %></p>
  </div>
<% end %>
