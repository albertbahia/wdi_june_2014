<h2>Groups</h2>

<div class="add_group"><%= link_to('Add Group', new_group_path) %></div>

<ul>
  <% @groups.each do |group| %>
  <li>
    <%= link_to(group.name, group_path(group)) %><br>
  </li>
  <% end %>
</ul>
