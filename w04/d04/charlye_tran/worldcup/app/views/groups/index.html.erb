<h2 class="head">Groups</h2>

<div class="index">
  <p><%= link_to('World Cup Fev.r', root_path) %> </p>
  <p><%= link_to('Add a New Group', new_group_path) %></p>

  <ul>
    <% @groups.each do |group| %>
      <%= link_to(group.name, group_path(group))%>
      <% end %>
  </ul>
</div>
