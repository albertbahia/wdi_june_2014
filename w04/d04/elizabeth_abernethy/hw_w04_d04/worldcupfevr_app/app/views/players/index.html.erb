<h2>Players</h2>

<%= link_to('Add a New Player', new_player_path) %>

<ul>
  <% @players.each do |player| %>
    <li> <%= link_to image_tag(player.photo_url, player_path(player)) %></li>
    <%= player.name %>
    <%= player.teams.country %>
  <% end %>
</ul>
