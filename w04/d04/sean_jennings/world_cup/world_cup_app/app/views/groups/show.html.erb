<p>Teams in <%= @group.name %></p>
<% @group.teams.each do |team| %>
  <ul>
    <li>
    <a href="/teams/<%= team.id %>"><%= team.country %></a>
    </li>
  </ul>
<% end %>

<%= link_to "Edit", edit_group_path %>
