<html>
  <head>
  </head>
  <body>
    <h3> Players: </h3>
    <% @players.each do |player| %>
    <ul>
      <li> <%= link_to(player.name, player_path(player)) %> </li>
    </ul>
    <% end %>

    <p> <%= link_to("Add new player", new_player_path) %> </p>
  </body>
</html>
