<h2>Songs</h2>

<%= link_to('New Song', new_song_path) %>

<ul>
  <% @songs.each do |song| %>
    <li>
      <%= link_to("#{song.title} by #{song.artist.name}", song_path(song)) %>
    </li>
  <% end %>
</ul>
