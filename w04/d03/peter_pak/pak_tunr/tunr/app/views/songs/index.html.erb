<h2>Songs</h2>

<%= link_to('Add New Song', new_song_path) %>

<ul>
  <% @song.each do |song| %>
    <li>
      <%= link_to("#{song.title} - #{song.artist.name}", song_path(song)) %>

    </li>
  <% end %>
</ul>
