<h2>Actors</h2>



<%= link_to('Add an Actor', new_actor_path) %>
<br>
<p>
  Click on the actor to find out more
  <ul>
    <% @actors.each do |actor| %>
    <li><%= link_to("#{actor.name}", actor_path(actor)) %></li>
    <% end %>
  </ul>
</p>
