  <div class="movie-trailer">
    <%= link_to("From the movie: #{@trailer.movie.title}", movie_path(@trailer.movie)) %>
    <h3><%= @trailer.title %></h3>
    <iframe width="420" height="315" src="<%= @trailer.embed_url %>" frameborder="0" allowfullscreen></iframe>
  </div>



  <%= link_to('Back to Trailers', trailers_path) %>
  <%= link_to('Edit', edit_trailer_path(@trailer)) %>
  <%= link_to('Delete', trailer_path(@trailer), {method: :delete}) %>
