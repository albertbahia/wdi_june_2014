<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Weather</title>

  <style>
    @import url(http://fonts.googleapis.com/css?family=Lato:100,300);
    .circle {
      border-radius: 100%;
      border: 2px solid black;
      width: 200px;
      height: 200px;
      position: fixed;
      margin: 172px 310px;
      background: black;
      color:white;
      text-align: center;
      font-size: 50px;
    }
    p {
      font-size: 25px;
      color: aquamarine;
      font-family: Lato;
      margin:10px 0 5px 0;
      font-weight: 100;
    }
    p:first-child {margin-top:35px;}
    img {
      position: absolute;
    }
    .name {
      color: black;
      font-size: 60px;
      position: absolute;
      z-index: 2;
    }
    input {
      width: 100%;
      height: 57px;
      border: none;
      font-size: 30px;
      font-weight: 100;
      text-align: center;
      color: grey;
    }
  </style>
</head>
<body>
  <%= yield %>
</body>
</html>
