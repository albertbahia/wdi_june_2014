<h2>Songs</h2>

<%= link_to('Add Song', new_song_path) %>

<ul>
  <% @songs.each do |song| %>
    <li>
      <%= link_to("#{song.title} - #{song.artist.name}", song_path(song)) %>
    </li>
  <% end %>
</ul>
