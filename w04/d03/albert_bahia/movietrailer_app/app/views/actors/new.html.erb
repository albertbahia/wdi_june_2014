<h2>Add Actor</h2>

<%= form_for @actor do |f| %>
  <%= f.label :photo_url %>
  <%= f.text_field :photo_url %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit('Add Actor') %>
<% end %>
