<h2>Add Trailer</h2>

<%= form_for @trailer do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :embed_url %>
  <%= f.text_field :embed_url %>

  <%= f.submit('Add Trailer') %>
<% end %>

<%= link_to 'Back to Trailers', trailers_path %>
