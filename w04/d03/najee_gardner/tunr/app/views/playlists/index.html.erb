<h2>Playlists</h2>

<%= link_to 'Add New Playlist', new_playlist_path %>

<ul>
  <% @playlists.each do |playlist| %>
    <li>
      <%= link_to playlist.name + " (" + playlist.songs.count.to_s + " songs)", playlist_path(playlist) %>
    </li>
  <% end %>
</ul>
