<h2>New Actor</h2>

<%= form_for(@actor) do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :photo_url %>
<%= f.text_field :photo_url %>

<%= f.label :movie_id %>
<%= f.text_field :movie_id %>

<%= f.submit %>

<% end %>

<%= link_to 'Back to Actor', actor_path(@actor) %>
