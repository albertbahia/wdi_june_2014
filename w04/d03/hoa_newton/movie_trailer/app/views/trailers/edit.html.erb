<%= link_to 'Back to this Trailer', movie_path(@trailer) %>

<%= form_for @trailer, method: :put do |f| %>
	Title: <%= f.text_field :title %>
	Embed URL: <%= f.text_field :embed_url %>
	
	<%= f.submit %>
<%end%>


<%= link_to 'Delete', trailer_path(@trailer), method: :delete %>