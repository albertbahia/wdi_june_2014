<h2>Artists</h2>

  <%= link_to('Add Artist', new_artist_path)%>

<ul>
  <% @artists.each do |artist| %>
    <li>
        <%= link_to(artist.name, artist_path(artists)) %>
        <%= artist.name %>
      </a>
    </li>
  <% end %>
</ul>
