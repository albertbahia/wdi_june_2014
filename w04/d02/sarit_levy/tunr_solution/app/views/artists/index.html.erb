<h2>Artists</h2>

<!-- <a href="/artists/new">Add Artist</a> --> #this is now replaced by:

<%= link_to('Add Artist', new_artist_path) %>  #this new_artist_path was named in the routes file

<ul>
  <% @artists.each do |artist| %>
    <li>
      <!-- <a href="/artists/<%= artist.id %>"> -->
      <%= link_to(artist.name, artist_path(artist)) %>
        <%= artist.name %>
      </a>
    </li>
  <% end %>
</ul>
