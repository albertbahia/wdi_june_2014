<h3>NEW TWEED</h3>

<%= link_to('HOME', tweeds_path) %>

<%= form_for(@tweed) do |f| %>

  <%= f.label(:content) %>
  <%= f.text_field(:content) %>

  <%= f.label(:image_url) %>
  <%= f.text_field(:image_url) %>

  <%= f.label(:author) %>
  <%= f.text_field(:author) %>

  <%= f.submit %>
<% end %>
