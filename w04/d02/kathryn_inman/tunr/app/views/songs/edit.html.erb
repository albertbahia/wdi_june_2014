<h2>Edit Song:</h2>

<!-- <form action="/songs/<%= @song.id %>" method="post">
  <label for="song[title]">Title:</label>
  <input name="song[title]" value="<%= @song.title %>">

  <label for="song[album]">Album:</label>
  <input name="song[album]" value="<%= @song.album %>">

  <label for="song[preview_url]">Preview:</label>
  <input name="song[preview_url]" value="<%= @song.preview_url %>">

  <label for="song[artist_id]">Artist's ID:</label>
  <input name="song[artist_id]" value="<%= @song.artist_id %>">

  <label for="song[price]">Price:</label>
  <input name="song[price]" value="<%= @song.price %>">

  <input type="submit" value="Submit Edits">
</form> -->

<%= form_for(@song) do |f| %>
<%= f.label(:title) %>
<%= f.text_field(:title) %>

<%= f.label(:album) %>
<%= f.text_field(:album) %>

<%= f.label(:preview_url) %>
<%= f.text_field(:preview_url) %>

<%= f.label("Artist ID") %>
<%= f.text_field(:artist_id) %>

<!-- <%= f.label(:price) %>
<%= f.text_field(:price) %> -->

<%= f.submit %>
<% end %>


<p><%= link_to("Back to songs.", songs_path) %></p>
<p><%= link_to("Delete song.", song_path(@song), {method: :delete}) %></p>
