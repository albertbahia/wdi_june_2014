<h3><%= @artist.name %></h3>
<p><%= @artist.nationality %></p>
<p><%= @artist.birthday %></p>

<img src="<%= @artist.photo_url %>" />

<p>Udated: <%= @artist.updated_at %></p>

<%= form_for 'Edit', edit_artist_path(@artist) %>
<%= form_for 'Delete', artist_path(@artist), method: :delete %>
