<h6><%= link_to('Back to Home', artists_path) %></h6>
<h2>Edit Painting</h2>

<%= form_for(@artist) do |f| %>
  <%= f.label(:img_url) %>
  <%= f.text_field(:img_url) %>

  <%= f.label(:title) %>
  <%= f.text_field(:title) %>

  <%= f.label(:year_painted) %>
  <%= f.text_field(:year_painted) %>

  <%= f.label(:description) %>
  <%= f.text_field(:description) %>

  <%= f.submit %>
<% end %>

<%= link_to('Back to Painting', panting_path(@painting)) %> |
<%= link_to('Delete this Artist', painting_path(@painting), method: :delete) %>
